# EventSlot Objective Status

Last updated: 2026-07-18

| Objective | Expected outcome | Status |
| --- | --- | --- |
| Hide payment functionality for now | Users should see “coming soon / working on this” instead of usable payment setup. | Improved and covered by live smoke plus regression tests. Dashboard-wide billing banner removed. Billing/payment pages remain informational. Billing page launch-interest form was removed so users cannot trigger a broken notify/save flow. Legacy credit/report purchase endpoints return coming-soon responses before auth, rate limiting, or Paystack checkout while billing is disabled. |
| Remove confusing super-admin payment banner | Super admin should not see billing restriction messaging on normal dashboard pages. | Improved. The global dashboard shell no longer injects the billing launch banner, and the billing page no longer shows super-admin-specific privilege copy. Live smoke now verifies payment purchase endpoints are paused. |
| Keep free event creation simple | Organizers should not be pushed into paid setup while creating a normal event. | Mostly done. Payment maintenance notices remain where needed, and paid checkout APIs are disabled. Needs final signed-in UI check. |
| Light/dark theme | Theme choice should work across the system, not only the landing page. | In progress. The shared dashboard shell, dashboard home/admin summaries, event list/detail/email tools, profile, feedback, insights, notifications, team, and sidebar install surfaces use semantic theme tokens. Every current admin route now uses the shared semantic palette, including the dashboard, users, health, launch, and internal payment-test screens. Core attendee workflows now follow the same palette across registration details/edit/success, post-event feedback, team invitation acceptance, ticket confirmation verification, and walk-in loading states. The sidebar refreshes its name/photo from the saved profile instead of relying only on a stale session. Public Terms, Privacy, unauthorized, not-found, global-error, and cache-recovery routes also follow the active theme. Organizer create/edit now use semantic accent, warning, and error tokens for their most visible selection, banner, CTA, and validation states. Remaining work is the page-by-page visual audit plus residual marketing/payment secondary surfaces and smaller hardcoded focus/hidden-branch accents. |
| Google sign-in terms/privacy consent | Users signing in with Google should agree to terms and privacy policy. | Implemented and covered by the Cloud Run smoke test, which checks the live sign-in/sign-up bundles for Google consent wording. |
| SMTP email service | Move away from Resend-only dependency and document SMTP setup. | Improved. SMTP docs, env examples, Nodemailer runtime support, shared SMTP provider-selection helpers, admin health checks, and a Cloud Run SMTP configuration helper exist. Provider selection is covered by regression tests for explicit SMTP, auto-SMTP with complete secrets, explicit Resend fallback, and sender normalization. Production still needs actual SMTP secrets before switching fully. |
| Registration draft by email | Attendee email should save/restore unfinished form progress. | Implemented. Draft API stores progress by event plus email. Live browser evidence now shows draft-save succeeding again on production after the registration-draft schema hotfix; resume-to-previous-step confirmation still needs a clean manual retest. |
| Waitlist promotion email | Promoted waitlisted attendee should receive a congratulations email and proceed to ticket. | Live-proven on July 18, 2026. A real attendee was waitlisted, promoted through the live capacity endpoint, and the API returned `attempted: 1`, `sent: 1`, `failed: 0`. The promotion email, waitlist email, and response-copy email were all confirmed in the real inbox at `eventslot.co@gmail.com`. |
| Reports free for organizers | Authorized organizers/team/admins should generate/download reports without payment. | Implemented, deployed, and covered by regression tests for free preview/download access, assigned team access, and unauthorized blocking. Report purchase routes are guarded during payment pause, report bundle cards are hidden while billing is paused, and assistant/docs copy now describes report downloads as free during the premium rollout. |
| Team member event access | Invited team member should only see assigned event. | Partially live-proven on July 18, 2026. Access scoping is implemented in code and covered by regression tests for explicit event assignment. Real invite delivery is confirmed (`danielmwalili1@gmail.com` received the team invite email), and the wrong-account handoff UX was fixed and deployed so `Sign out and continue` now returns to `/signin?callbackUrl=...` instead of looping the super-admin session. The remaining live blocker is Google-account acceptance for the invited user: after selecting `danielmwalili1@gmail.com`, the browser consistently shows a `502 Server Error` page on `/api/auth/callback/google`, and the session does not persist back into `/team/accept`. Cloud Run request logs for the reproduced attempts at `2026-07-18T09:17:16Z` and `2026-07-18T09:23:28Z` still recorded the callback endpoint as `302`, so assigned-event visibility is not yet fully live-proven and the Google callback/session handoff needs another fix. |
| Account deletion | Super admin and users should be able to delete test/user accounts safely. | Improved with cleanup and covered by regression tests for self-delete plus admin-delete event handling. Live evidence on July 18, 2026 now confirms the self-delete surface is present on `/dashboard/profile`: the danger zone renders, `Delete my account` is visible, and the destructive confirmation modal opens with the expected permanent-delete wording. A final destructive live delete was not executed yet, and the super-admin delete path still needs a live non-destructive walkthrough or disposable-user test. |
| Verify-ticket workflow documentation | Document current scanner/search/upload behavior and future verifier subdomain idea. | Done in `docs/VERIFY_TICKET_WORKFLOW.md`. |
| 100,000 registrations/week readiness | Do not claim capacity until load tested. | In progress. A load-test plan and guarded helper script now cover page reads, draft saves, registration submit, and waitlist promotion. Actual staging/prod-safe load run with metrics still required. |

## Automated Live Checks

`npm run test:cloudrun -- --base-url=https://www.eventsslot.com` now checks:

- Homepage availability and absence of old payment-marketing phrases.
- Sign-in and sign-up availability plus deployed Google Terms/Privacy consent wording.
- Direct credit and report-download purchase endpoints return `503` while payments are paused.
- `robots.txt` and `sitemap.xml` remain valid.
