# Registration Closed Parity

Tracks the attendee-facing closed-registration behavior so web and mobile keep event details, maps, and organizer contact visible while registration actions switch to an `Event closed` state.
